var add = function(number1, number2) {
	return number1 + number2;
};

var number1 = parseInt(prompt("Enter a number for addition:"));
var number2 = parseInt(prompt("Enter another number for addition:"));

alert(add(number1, number2));

var subtract = function(number1, number2) {
	return number1 - number2;
};

var number1 = parseInt(prompt("Enter a number for subtraction:"));
var number2 = parseInt(prompt("Enter another number for subtraction:"));

alert(subtract(number1, number2));

var multiply = function(number1, number2) {
	return number1 * number2;
};

var number1 = parseInt(prompt("Enter a number for multiplication:"));
var number2 = parseInt(prompt("Enter another number for multiplication:"));

alert(multiply(number1, number2));

var divide = function(number1, number2) {
	return number1 / number2;
};

var number1 = parseInt(prompt("Enter a number for division:"));
var number2 = parseInt(prompt("Enter another number for division:"));

alert(divide(number1, number2));
